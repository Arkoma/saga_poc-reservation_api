#spring:
#  kafka:
#    consumer:
#      auto-offset-reset: earliest
#      group-id: baeldung
#    template:
#      default-topic: embedded-test-topic
spring:
  kafka:
    consumer:
      auto-offset-reset: earliest
      group-id: baeldung
      enable-auto-commit: false
kafka:
  topicName: embedded-test-topic